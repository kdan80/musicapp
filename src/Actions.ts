// Get source for current track
// React.useEffect(() => {
//     if (nowPlaying) {
//         const src = nowPlaying.presignedUrls[currentTrack]
//         setAudioSrc(src)
//     }
// }, [currentTrack, nowPlaying])
